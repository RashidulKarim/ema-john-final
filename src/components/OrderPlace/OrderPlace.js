import React from 'react';
import image from '../../images/giphy.gif';

const OrderPlace = () => {
    return (
        <div>
            <img src={image} alt="" />
        </div>
    );
};

export default OrderPlace;